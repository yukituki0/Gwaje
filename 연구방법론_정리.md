# 딥러닝 기반 동적 공격 그래프 경로 분석 및 방어 전략 시각화 연구

## 목차

- [방법론 정리 (2026.08 기준)](#방법론-정리-202608-기준)
- [⭐ 최종 채택 방법론 요약 (보고서 작성 시 이 내용을 기준으로 할 것)](#최종-채택-방법론-요약-보고서-작성-시-이-내용을-기준으로-할-것)
- [0. 제목](#0-제목)
- [1. 연구 차별화 지점](#1-연구-차별화-지점)
  - [1.1 선행연구 검토 결과 (중요)](#11-선행연구-검토-결과-중요)
  - [1.1.1 공격 그래프 + GNN 계열 선행연구 심층 한계 분석](#111-공격-그래프-gnn-계열-선행연구-심층-한계-분석)
  - [1.2 연구 포지셔닝 (확정)](#12-연구-포지셔닝-확정)
  - [1.3 실제성(현실성) 확보 방안](#13-실제성현실성-확보-방안)
- [2. 전체 파이프라인 (4단계)](#2-전체-파이프라인-4단계)
- [3. 이론적 배경 요약](#3-이론적-배경-요약)
- [4. 세부 설계 (오늘 확정된 핵심 내용)](#4-세부-설계-오늘-확정된-핵심-내용)
  - [4.1 노드 입력 Feature (5종, 기존 계획 유지)](#41-노드-입력-feature-5종-기존-계획-유지)
  - [4.2 엣지 Feature](#42-엣지-feature)
  - [4.3 risk_score (GAT 학습 라벨) — 최종 정의](#43-risk_score-gat-학습-라벨-최종-정의)
  - [4.3.1 가중치(w1/w2/w3) 초기값 (확정)](#431-가중치w1w2w3-초기값-확정)
  - [4.4 "라벨이 이미 그래프 정보를 다 담고 있다면 왜 GAT가 필요한가?"](#44-라벨이-이미-그래프-정보를-다-담고-있다면-왜-gat가-필요한가)
  - [4.5 GAT 모델 구조](#45-gat-모델-구조)
- [5. MITRE ATT&CK 기반 공격 체인 (확정)](#5-mitre-attck-기반-공격-체인-확정)
- [6. 전체 네트워크 구조 설계 — "골격 고정 + 통제된 다양성"](#6-전체-네트워크-구조-설계-골격-고정-통제된-다양성)
  - [6.1 1층: 고정 골격 (표준 기업 네트워크 참조 구조)](#61-1층-고정-골격-표준-기업-네트워크-참조-구조)
  - [6.2 2층: 통제된 다양성 (규모 확장)](#62-2층-통제된-다양성-규모-확장)
  - [6.3 노드 구성안 (총 18개)](#63-노드-구성안-총-18개)
  - [6.4 제로 트러스트(Zero Trust) 요소 반영 (단계적 적용)](#64-제로-트러스트zero-trust-요소-반영-단계적-적용)
- [7. LLM 기반 방어 전략 생성 파이프라인](#7-llm-기반-방어-전략-생성-파이프라인)
  - [7.1 모델 선택: 로컬 모델 우선](#71-모델-선택-로컬-모델-우선)
  - [7.2 정답 기준표 (검증용, CISA 공식 근거)](#72-정답-기준표-검증용-cisa-공식-근거)
  - [7.3 프롬프트 구조](#73-프롬프트-구조)
  - [7.4 평가(검증) 방식 — 체크리스트 매칭](#74-평가검증-방식-체크리스트-매칭)
  - [7.5 확장 가능한 파이프라인 설계 (노드 수 증가 대응)](#75-확장-가능한-파이프라인-설계-노드-수-증가-대응)
- [8. GNN 확장성 및 데이터 설계 원칙](#8-gnn-확장성-및-데이터-설계-원칙)
  - [8.1 GAT의 구조 유연성 (Inductive vs Transductive)](#81-gat의-구조-유연성-inductive-vs-transductive)
  - [8.2 데이터는 "양보다 위험도 분포의 균형"](#82-데이터는-양보다-위험도-분포의-균형)
  - [8.3 단일 MITRE ATT&CK 체인 사용에 따른 과적합 우려 및 대응](#83-단일-mitre-attck-체인-사용에-따른-과적합-우려-및-대응)
- [9. 프로젝트 구조 (Python 기반, 웹 연동 고려)](#9-프로젝트-구조-python-기반-웹-연동-고려)
  - [9.1 디렉토리 구조](#91-디렉토리-구조)
  - [9.2 학습(Training)과 추론(Inference)의 완전한 분리](#92-학습training과-추론inference의-완전한-분리)
- [10. 작업 순서 (우선순위 기준, 날짜 대신 의존관계로 관리)](#10-작업-순서-우선순위-기준-날짜-대신-의존관계로-관리)
  - [필수 (MVP)](#필수-mvp)
  - [있으면 좋음 (시간 남는 순서대로)](#있으면-좋음-시간-남는-순서대로)
- [11. 남은 것 / 다음에 다룰 것](#11-남은-것-다음에-다룰-것)
- [12. 참고문헌 (2026.08 기준 확인된 것)](#12-참고문헌-202608-기준-확인된-것)
- [13. 구현 진행상황 및 실험 결과 (2026.08, 실제 코드 실행 기반)](#13-구현-진행상황-및-실험-결과-202608-실제-코드-실행-기반)
  - [13.1 최종 프로젝트 구조 (계획 대비 변경됨)](#131-최종-프로젝트-구조-계획-대비-변경됨)
  - [13.2 탐지 파이프라인 구현 결과](#132-탐지-파이프라인-구현-결과)
  - [13.3 GAT 학습 및 검증 결과](#133-gat-학습-및-검증-결과)
  - [13.4 LLM 방어전략 — 로컬 Qwen에서 Gemini API로 전환](#134-llm-방어전략-로컬-qwen에서-gemini-api로-전환)
  - [13.5 웹 시각화 구현 결과](#135-웹-시각화-구현-결과)
  - [13.6 종합 현황](#136-종합-현황)
- [14. 지도교수 피드백 반영 및 연구 재설계 (2026.08, 콜로키움 이후)](#14-지도교수-피드백-반영-및-연구-재설계-202608-콜로키움-이후)
  - [14.1 지도교수 피드백 요약](#141-지도교수-피드백-요약)
  - [14.2 문헌조사 결과 요약 (Advanced Research 수행)](#142-문헌조사-결과-요약-advanced-research-수행)
  - [14.3 즉시 반영: risk_score → 외부 라벨(EPSS/KEV) 전환](#143-즉시-반영-risk_score-외부-라벨epsskev-전환)
  - [14.4 GAT(정적 모델) 유지 여부에 대한 결정](#144-gat정적-모델-유지-여부에-대한-결정)
  - [14.6 최종 전환: vuln-net-ag(검증된 생성기) 기반 실제 데이터셋으로 이동](#146-최종-전환-vuln-net-ag검증된-생성기-기반-실제-데이터셋으로-이동)
  - [14.8 규모 확장 실험: 학습 데이터 크기와 일반화 성능의 관계](#148-규모-확장-실험-학습-데이터-크기와-일반화-성능의-관계)
  - [14.10 모델·손실함수 개선: GATv2→Graph Transformer, MSE→BCE](#1410-모델손실함수-개선-gatv2graph-transformer-msebce)
  - [14.10.1 Ablation 실험: 모델과 손실함수의 개별 기여도 분리](#14101-ablation-실험-모델과-손실함수의-개별-기여도-분리)
  - [14.10.3 참고: 공격그래프+GNN 분야에서 GAT의 위치, Dijkstra의 최신 대안](#14103-참고-공격그래프gnn-분야에서-gat의-위치-dijkstra의-최신-대안)
  - [14.11 LLM 재검토 — 결론: 기존 설계가 이미 "근거 주입" 요건을 충족함](#1411-llm-재검토-결론-기존-설계가-이미-근거-주입-요건을-충족함)
  - [14.13 Dijkstra-GAT 폐루프 구현: GAT 위험도의 엣지 가중치 피드백](#1413-dijkstra-gat-폐루프-구현-gat-위험도의-엣지-가중치-피드백)
  - [14.14 CWE(취약점 유형) 원-핫 특성 추가](#1414-cwe취약점-유형-원-핫-특성-추가)
  - [14.15 다음 우선순위 (갱신)](#1415-다음-우선순위-갱신)

---

## 방법론 정리 (2026.08 기준)

---

## ⭐ 최종 채택 방법론 요약 (보고서 작성 시 이 내용을 기준으로 할 것)

**이 문서는 시행착오 과정을 순서대로 기록한 것이라, 앞부분(1~13장)에는 이후 폐기되거나 대체된 접근이 많이 포함되어 있음. 최종 보고서에는 아래 요약과 14장 내용을 기준으로 작성할 것.**

- **그래프 데이터**: 직접 설계한 18노드 그래프(5~6장) 대신, **vuln-net-ag(Palma & Bonomi, 2025)** — 실제 NVD 데이터 기반 검증된 네트워크 생성기 — 의 결과물을 사용 (14.6절)
- **학습 라벨(정답)**: 직접 정의한 risk_score 휴리스틱 공식(4.3절) 대신, **FIRST.org EPSS(실제 악용확률)와 CISA KEV(실제 야생 악용 확인)** 외부 데이터를 그대로 사용 (14.3절)
- **탐지 모델**: GATv2 (2~4장 구조는 유지, 입력·라벨 출처만 교체)
- **검증 방식**: 학습에 전혀 사용하지 않은 규모의 네트워크(held-out)로 스피어만 상관계수 검증 — 학습 규모를 90노드→270노드로 늘리자 상관계수 0.17→0.85로 개선 확인 (14.8절)
- **방어전략**: Google Gemini API(무료 티어)로 CISA 공식 가이드 기반 방어전략 생성, 사전 생성 후 웹앱은 조회만 수행 (13.4절, 9장)
- **연구 전환 계기**: 지도교수 피드백(이너루프/합성데이터/근거없는 임의값 문제 지적) — 전체 배경은 **14장**에 기록

**읽는 순서 추천**: 처음 읽는다면 14장부터 읽고, 필요할 때만 1~13장(왜 이런 시행착오를 거쳤는지, 어떤 대안들을 검토했는지의 근거)을 참고할 것.

---

## 0. 제목

- **한글**: 딥러닝 기반 동적 공격 그래프 경로 분석 및 방어 전략 시각화 연구
- **영문**: A Study on Path Analysis and Defense Strategy Visualization of Dynamic Attack Graphs Using Deep Learning

기존 "앙상블" 중심 표현에서 "딥러닝" 중심의 포괄적 표현으로 변경. 본문 방법론 뼈대는 유지.

---

## 1. 연구 차별화 지점

### 1.1 선행연구 검토 결과 (중요)

검색 결과 **Kumar & Namdeo (2025), "Adaptive Cyber Threat Prediction Using Attack Graphs and Real-Time ML Integration"** 논문이 Dijkstra + GNN + CVE/NVD 연동이라는 유사한 골격을 이미 제시함.

| 구분 | 선행연구 (Kumar & Namdeo) | 본 연구 |
|---|---|---|
| GNN-Dijkstra 관계 | 병렬 비교 (RF/LSTM과 성능만 비교) | GNN 위험도가 엣지 가중치에 반영되어 Dijkstra 재실행 (실질적 결합) |
| 결과 활용 | 예측/알림에서 종료 | LLM 기반 방어 전략 자동 생성까지 연결 |
| 사용자 접근성 | 없음 (SIEM/API 중심) | 비전공자용 웹 시각화 |

### 1.1.1 공격 그래프 + GNN 계열 선행연구 심층 한계 분석

콜로키움 이후 지도교수 피드백(14장)에 대응하며 추가로 확인한 선행연구들의 데이터·라벨 방식과 명시적 한계를 정리함. 이 분석이 본 연구가 채운 구체적 공백(EPSS/KEV 외부 라벨, vuln-net-ag 기반 실제 데이터)의 근거가 됨.

| 연구 | 방법 | 데이터(합성/실제) | 학습 라벨/정답 정의 | 논문이 인정한/드러난 한계 |
|---|---|---|---|---|
| **VulRG** (Jiang et al., arXiv:2502.11143, 2025) | 그래프 기반 다층 패치 우선순위 | 3개 시나리오, 기존 논문 사례 각색+실제 CVE | **외부 라벨 없음.** 휴리스틱 가중합(CVSS·EPSS·Exploit·centrality). 검증은 출처 논문 전문가 코멘트와 정성 일치 | 정량 ground truth 검증 없음, 일반화 미검증 — 본 연구의 원래 risk_score 공식(4.3절)과 동일한 한계를 공유했음을 자체 확인 |
| **DSRD / Topology-Aware** (Future Internet 2026, 18(6):283) | Kirchhoff 유효저항, EPSS를 엣지 컨덕턴스로 사용 | 9개 토폴로지, 실제 CVE(cvelistV5) | **무학습.** 라벨 불필요한 구조지표 | "실제 침해로 검증되지 않은 확장된 개념증명"이라 스스로 명시. GNN을 아예 포기하고 무학습 지표로 우회한 사례 |
| **Kumar & Namdeo** (IJNRD 2025) | Dijkstra+GNN+실시간 위협 인텔 API | DARPA IDS, CICIDS2017 로그 | 과거 공격 발생 기반 확률 스코어 | Dijkstra-GNN이 병렬(폐루프 아님), 저널 등급 낮아 수치 신뢰도 제한적 |
| **Lu et al.** (Ranking Attack Graphs with GNN, ISPEC 2009) | GNN이 랭킹 함수 학습 | 합성 소규모 공격그래프 다수 | **자체 생성.** 기존 랭킹기법(Mehta et al.)으로 라벨 생성 → GNN이 재현 | **본 연구의 초기 설계(4.3절)와 구조적으로 거의 동일** — 15년 전 연구와 같은 이너루프 패턴을 답습했었음을 자인하는 근거 |
| **TargetNodeRank** (Springer 2025) | PageRank+betweenness 타겟특화 랭킹 | 다양한 공격그래프 | Brute force 근사, 무학습 휴리스틱 | PageRank 단독의 타겟 무관성만 지적, 외부 검증 라벨 부재는 동일 |
| **TriAssetRank** (IEEE TIFS 2024) | 취약점·권한·익스플로잇 3자 랭킹 | 모델 네트워크 | AssetRank(PageRank류) 확장 | CVSS의 관계 무시 문제는 지적하나 자체도 외부 검증 라벨 없음 |
| **GNN-IDS** (ARES 2024) | 공격그래프+실시간 측정→GNN 침입탐지 | 실제 소규모 네트워크 차용+합성 데이터 | 침입 이진 라벨(합성) | 합성 데이터 의존, 실제 대규모 미검증 |

**핵심 관찰**: 표에서 확인되듯, 이 분야의 절대다수 선행연구(VulRG, DSRD, Lu et al., TargetNodeRank, TriAssetRank)가 **외부에서 독립적으로 검증된 라벨 없이, 자체 정의한 휴리스틱/랭킹을 정답으로 삼아 순환적으로 검증**하는 공통 한계를 가짐. 특히 Lu et al.(2009)은 본 연구의 초기 설계(risk_score 휴리스틱을 GAT가 재현, 4.3절)와 구조적으로 거의 동일하여, **"외부 검증 라벨 부재"라는 문제가 15년 넘게 이 분야에서 반복되어 왔음**을 보여줌.

**본 연구가 메우는 공백**: "외부에서 독립 검증된 라벨(FIRST.org EPSS, CISA KEV)로 GNN 기반 위험도 예측을 검증한 연구가 이 분야에 사실상 없다"는 지점을 14.3·14.6절의 전환으로 메움. 또한 Kumar & Namdeo가 병렬로만 다룬 Dijkstra-GNN 관계를 실질적 결합(엣지 가중치 피드백)으로 발전시키는 것을 향후 과제(14.9절)로 설정함.

### 1.2 연구 포지셔닝 (확정)

**"탐지 성능 자체를 새로 증명"하는 연구가 아니라, "탐지 → 방어전략 → 시각화"로 이어지는 완결된 파이프라인/시스템을 제시하는 연구**로 방향을 잡음.

- 탐지 단계(Dijkstra+GAT)는 기존 방법론을 재구현하여 사용, 베이스라인 비교실험은 최소화
- 신규 기여는 **LLM 기반 방어전략 자동생성**과 **비전공자용 웹 시각화**에 집중

### 1.3 실제성(현실성) 확보 방안

- 원래 계획했던 VM 기반 실제 침투테스트(Metasploit 등)는 **일정(1~2주) 및 실습 경험 부족으로 제외**
- 대신 **MITRE ATT&CK에 문서화된 실제 공격 체인(TTP)을 그래프 시나리오로 그대로 이식** → "가상 환경이지만 실제 공격 패턴을 반영했다"는 근거 확보
- 방어 전략은 **NIST / CIS Benchmark 가이드라인과 대조**하여 타당성 점검

> 계획서에 VM 관련 내용이 있었다면 "시간 제약으로 VM 기반 실증은 향후 과제로 남기고, MITRE ATT&CK 기반 공격 체인 이식으로 대체함"이라고 명시.

---

## 2. 전체 파이프라인 (4단계)

```
[1] MITRE ATT&CK 기반 시나리오 → 동적 공격 그래프 생성
        ↓
[2] Dijkstra + GAT 탐지 (위험 경로·노드 도출)
        ↓
[3] LLM 기반 방어 전략 자동 생성 (NIST/CIS 대조 검증)
        ↓
[4] 웹 기반 시각화 (위험도 표시, 방어전략 표시)
```

---

## 3. 이론적 배경 요약

| 개념 | 핵심 내용 |
|---|---|
| 그래프 | 노드(시스템/계정) + 엣지(접근 가능성), 유방향·가중치 그래프 사용 |
| 공격 그래프 | 노드=시스템 상태, 엣지=취약점을 이용한 상태 전이 |
| 동적 그래프 | 타임스텝마다 패치·침해·신규장비 등 이벤트 반영, 정적 그래프의 한계 보완 |
| Dijkstra | 시작 노드(공격자)로부터 최소 비용 경로 탐색. 가중치 = `attack_cost × (1 - success_prob)` |
| GNN | 메시지 패싱을 통해 이웃 노드 정보를 반영하여 각 노드 표현을 업데이트 |
| GAT | 이웃마다 attention 가중치를 학습해 차등 반영 (비균질적 공격 그래프에 적합) |

---

## 4. 세부 설계 (오늘 확정된 핵심 내용)

### 4.1 노드 입력 Feature (5종, 기존 계획 유지)
`vulnerability_score, privilege_level, importance, is_compromised, patch_status`

### 4.2 엣지 Feature
`attack_cost, success_prob, protocol(원-핫 인코딩)`

- protocol은 범주형이므로 원-핫 인코딩 처리 (예: SSH/RDP/SMB/HTTP 등 시나리오 설계 시 미리 종류 고정, 필요시 "기타" 카테고리 추가)

### 4.3 risk_score (GAT 학습 라벨) — 최종 정의

> ⚠️ **이 절의 risk_score 공식은 14.3~14.6절에서 폐기되고 EPSS/KEV 외부 라벨로 대체되었음. 보고서 작성 시 이 절 대신 14.3절·14.6절을 인용할 것. 이 절은 "왜 처음에 이렇게 설계했고 무엇이 문제였는지"의 기록으로만 유지.**

```
risk_score = w1 × vulnerability_score (자신의 취약점)
           + w2 × (1 / Dijkstra_distance_from_attacker)   (접근 용이성, predecessor 방향)
           + w3 × Weighted PageRank 기반 전파값             (downstream 확산 위험, successor 방향)
```

**설계 근거 (해결한 3가지 쟁점):**

1. **순환 정의 문제**: "이웃 위험도 평균"을 그대로 쓰면 순환 참조가 발생 → **반복 전파(PageRank류)** 방식으로 해결. NetworkX의 `pagerank()` 계열 함수 활용 가능
2. **그래프 방향성**: predecessor(공격자가 나에게 오는 방향)와 successor(내가 뚫리면 퍼지는 방향)는 의미가 다름 → 두 항으로 분리 (w2=predecessor 기반 접근용이성, w3=successor 기반 확산위험)
3. **엣지 가중치 미반영 문제**: 단순 이웃 평균은 "연결 강도(성공확률)"를 무시함 → **Weighted PageRank**로 success_prob을 전파 가중치에 반영
   ```
   v → u로의 전파 영향력 = risk(v) × success_prob(v→u) / (v의 나가는 성공확률 총합)
   ```

**선행연구 근거 (2026.08 추가 확인)**

| 요소 | 근거 문헌 | 인용 내용 |
|---|---|---|
| PageRank 기반 전파 위험도 | TargetNodeRank (Springer) | 공격 그래프에서 PageRank를 응용해 노드 중요도를 순위화하는 것은 검증된 기법이며, 매개중심성(betweenness centrality)과 결합해 특정 목표 노드 기준 중요도를 정교화하는 방식이 제안됨 |
| "직접위험 + 전파위험" 가중합 구조 | VulRG (arXiv, 2025) | 자산 위험도를 직접 위험(exploit likelihood, CVSS 기반)과 전파 위험(propagation likelihood)의 가중합으로 계산 — 본 연구의 w1(직접) + w2/w3(전파·접근성) 구조와 동일한 설계 패턴 |
| GATv2 (edge-aware) 채택 | Brody et al. (GATv2 원논문); 네트워크 공격 지식추론 적용사례 (Nature Sci. Rep., 2025) | GATv2는 표준 GAT 대비 표현력이 강화된 구조로, 이미 네트워크 공격 그래프 도메인에 적용된 사례가 있음 |

> 즉 risk_score 공식은 임의로 조합한 것이 아니라, **TargetNodeRank(PageRank 응용)와 VulRG(직접+전파 위험 가중합 구조)** 두 선행연구의 접근을 결합·응용한 것으로 서술 가능.

### 4.3.1 가중치(w1/w2/w3) 초기값 (확정)

```
w1 = 0.3  (자기 취약점 — 기본 심각도는 반영하되, GAT가 이웃 정보 없이 답을 맞히지 못하도록 억제)
w2 = 0.3  (접근 용이성 — 공격자로부터 실제로 얼마나 쉽게 도달 가능한가)
w3 = 0.4  (다운스트림 전파위험 — 본 연구의 핵심 기여점(동적 전파 반영)이므로 최고 비중 부여)
```

- 세 항목을 극단적으로 몰지 않고 균형 있게 배분하되, 연구의 핵심 강조점인 전파위험(w3)에 소폭 가중
- VulRG 논문에서도 직접위험 계산 시 유사 수준(0.3/0.4/0.3)으로 요소를 배분한 방식을 참고
- 실험 단계에서 조정 가능한 초기값으로 설정

### 4.4 "라벨이 이미 그래프 정보를 다 담고 있다면 왜 GAT가 필요한가?"

> risk_score(라벨)는 **학습 데이터를 만들 때만** 전체 그래프 구조(Dijkstra, PageRank)를 사용해 계산한 값이고, GAT는 이를 **각 노드의 지역적 feature만으로 근사**하도록 학습됨. 학습 후에는 그래프가 변할 때마다 전체 재계산 없이 **빠른 추론**이 가능 — 이것이 GAT의 실질적 존재 이유(동적 환경에서의 효율성).

### 4.5 GAT 모델 구조

- 라벨 설계가 엣지 정보(success_prob 등)를 반영하므로, **모델 입력도 엣지 정보를 함께 사용하는 edge-aware 구조**로 설계해야 논리적 일관성이 생김
- PyTorch Geometric의 `GATv2Conv` 또는 `TransformerConv` 등 `edge_attr`을 입력받는 레이어 사용

---

## 5. MITRE ATT&CK 기반 공격 체인 (확정)

> ⚠️ **이 5장과 다음 6장(직접 설계한 18노드 그래프)은 14.6절에서 vuln-net-ag 기반 그래프로 대체되었음. 보고서의 "데이터/그래프 구성" 부분은 14.6절을 기준으로 작성할 것. 이 장들은 초기 설계 근거와 시행착오 기록으로만 유지.**

실제 침해사고 분석 연구(SecTracer 등)에서 흔히 매핑되는 표준 단계를 따르며, 각 단계에 **재현성·검증가능성이 높은 대표적 CVE**를 배정함 (선정 기준: 실제 대규모 침해사고에 사용되었고, 자료·CVSS·방어가이드가 풍부하게 공개된 사례).

| 단계 | ATT&CK Tactic | Technique | CVE / 근거 | 노드 매핑 |
|---|---|---|---|---|
| 1 | Initial Access | T1190 | **CVE-2021-26855** (ProxyLogon, MS Exchange, CVSS 9.8) — HAFNIUM 등 실제 국가배후 공격군이 사용, MITRE ATT&CK 공식 인용 | 외부 공격자 → DMZ 웹서버 |
| 2 | Discovery | T1046 / T1018 | 특정 CVE 아님 — 스캐닝 기법(Nmap류) | DMZ 웹서버 내부 탐색 |
| 3 | Credential Access | T1003 / T1555 | 특정 CVE 아님 — 자격증명 덤핑/탈취 기법 | DMZ → 내부서버 진입 조건 확보 |
| 4 | Lateral Movement | T1021 | **CVE-2017-0144** (EternalBlue, SMB) — WannaCry에 악용된 대표적 횡적이동 취약점 | 내부서버 → 사용자 PC |
| 5 | Privilege Escalation | T1078 | **CVE-2020-1472** (Zerologon) — 도메인 컨트롤러 권한 탈취 대표 사례 | 내부서버 관리자 권한 획득 |
| 6 | 목표 도달 | - | - | 내부서버 → 핵심 DB서버 |

각 단계(엣지)에 실제 CVE를 배정하여 "임의의 그래프"가 아닌 "실제 공격 체인의 이식"이라는 근거 확보. 참고: SecTracer 연구에서도 유사한 단계 매핑(Initial Access→Discovery→Credential Access/Discovery→Lateral Movement/Privilege Escalation)을 사용함.

---

## 6. 전체 네트워크 구조 설계 — "골격 고정 + 통제된 다양성"

6단계 메인 공격 경로 하나만으로는 Dijkstra(여러 후보 경로 중 최적 선택)와 GAT(다양한 패턴 학습) 둘 다 의미 있는 문제가 되지 않음 → 노드/경로를 확장하되, 완전 무작위가 아닌 2단계 구조로 설계.

### 6.1 1층: 고정 골격 (표준 기업 네트워크 참조 구조)

```
[외부(인터넷)]
     ↓ (방화벽)
[DMZ] — 웹서버, 메일서버, VPN 게이트웨이 등
     ↓ (내부 방화벽/세그멘테이션)
[내부망] — 업무서버, 파일서버, 사용자 PC
     ↓
[핵심자산] — DB서버, 도메인 컨트롤러, 관리자 계정
```

이 골격(DMZ-내부망 분리, 핵심자산 위치)은 실제 기업 네트워크 설계 원칙을 따르는 것이며, 5장의 6단계 메인 공격경로가 이 골격 위에 그대로 얹힘.

### 6.2 2층: 통제된 다양성 (규모 확장)

- **규모**: 노드 15~20개 (DMZ 3~4대, 내부서버 5~6대, PC 5~6대, 핵심자산 1~2개, 네트워크장비 1~2개)
- 메인 경로 외 노드에는 **NVD에서 실제 존재하는 CVE 중 심각도가 낮거나 시나리오와 무관한 것들을 배정** → 값 자체는 실제 CVE 기반이므로 완전 가짜 데이터 아님
- 일부는 이미 패치되어 막힌 경로, 일부는 우회 가능하지만 비용이 큰 경로 등을 섞어 Dijkstra가 "여러 후보 중 최적 선택"을 하는 실질적 문제가 되도록 구성

> 방법론 서술 시: "골격은 표준 네트워크 설계 원칙을 따르고, 세부 노드/속성 채움은 실제 CVE 데이터 기반의 통제된 범위 내에서 다양성을 부여했다"고 명시 — 완전 무작위(재현성·설득력 저하)도, 완전 고정(데이터 부족)도 아닌 절충안.

### 6.3 노드 구성안 (총 18개)

| 구역 | 개수 | 노드 예시 | 역할 |
|---|---|---|---|
| 외부 | 1 | 외부 공격자 | 시작점 |
| DMZ | 3 | 웹서버(ProxyLogon 취약, 메인경로), 메일서버(패치됨, 안전), VPN게이트웨이(중간 위험도) | 메인경로 1 + 가짜경로 2 |
| 내부서버 | 5 | 업무서버A(메인경로, EternalBlue 취약), 파일서버, 프린트서버, 웹앱서버, 백업서버 | 메인경로 1 + 배경 4 |
| 사용자 PC | 5 | PC-1~5 (1~2대만 메인경로 연결, 나머지는 막다른 길) | |
| 핵심자산 | 2 | 도메인 컨트롤러(메인경로, Zerologon 취약), DB서버(최종 목표) | |
| 네트워크장비 | 2 | 내부 라우터, 백업 스위치 | 배경 |

메인 공격경로(5장) 관여 노드는 4~5개, 나머지 13~14개는 "그럴듯한 배경 + 가짜경로" 역할.

**가짜경로/배경 노드의 역할**: 패치가 잘 된 노드(예: 메일서버)는 Dijkstra가 "비용이 커서 피해야 할 경로"로 학습할 소재가 되고, 막다른 길(예: 파일서버)은 "위험해 보이지만 핵심자산과 무관한 노드"를 GAT가 구분하는 능력을 검증하는 소재가 됨.

### 6.4 제로 트러스트(Zero Trust) 요소 반영 (단계적 적용)

전통적 DMZ/내부망 구조는 "경계 안쪽은 신뢰한다"는 전제가 있으나, 최근에는 이 전제가 흔들리는 추세(클라우드/재택근무 확산, 내부자 위협). NIST SP 800-207에 표준화된 개념.

- **1단계 (현재 적용)**: 전체 그래프 구조는 유지하되, 내부망 내부 엣지에도 "자동 신뢰"가 아닌 개별 인증 비용을 반영 — 내부 엣지의 success_prob을 무조건 높게 주지 않고 다소 낮게 설정하여 "내부라고 무조건 쉬운 것은 아니다"라는 요소 반영
- **2단계 (향후 확장 과제)**: DMZ/내부망 경계가 없는 완전한 제로 트러스트 구조의 별도 시나리오를 추가로 설계하여, 전통적 구조와 비교 실험 — 시간 여유가 있을 경우 시도, 없으면 결론부에 "향후 연구" 방향으로 명시

---

## 7. LLM 기반 방어 전략 생성 파이프라인

### 7.1 모델 선택: 로컬 모델 우선

| 기준 | 로컬 모델 (Qwen2.5 등) | API (Claude/GPT 등) |
|---|---|---|
| 비용 | 무료 (워크스테이션 활용) | 사용량 과금, 예산 불확실 |
| 재현성 | 높음 — 동일 환경에서 재현 가능 | 버전 변경 시 재현 어려움 |
| 계획서 정합성 | 기존 예산계획(5.2.1 워크스테이션)과 일치 | 별도 처리 필요 |

**결론**: 워크스테이션에서 **Qwen2.5-7B-Instruct** 등 로컬 모델을 우선 사용. 단, 워크스테이션 사용 불가 상황을 대비해 **모델 호출부를 교체 가능한 조각으로 분리**:

```
generate_defense(prompt) 함수로 모델 접근을 감쌈
   ├─ 옵션 A: 워크스테이션 Qwen2.5-7B-Instruct (transformers/vllm)
   ├─ 옵션 B: 개인 노트북용 경량 모델 (Qwen2.5-1.5B~3B, llama.cpp 양자화)
   └─ 옵션 C: 최후 수단으로 무료/저가 API 티어
```
모델을 갈아끼워도 프롬프트 생성/저장/평가 코드는 그대로 재사용되도록 설계.

### 7.2 정답 기준표 (검증용, CISA 공식 근거)

| CVE | 공식 출처 | 핵심 대응 조치 |
|---|---|---|
| CVE-2021-26855 (ProxyLogon) | CISA AA21-062A | Exchange 패치 즉시 적용, Test-ProxyLogon.ps1로 침해 스캔, 필요시 EOMT 사용 |
| CVE-2017-0144 (EternalBlue) | CISA WannaCry 권고, MS17-010 | 패치 적용, SMBv1 비활성화, 포트 445/137-139 경계 차단, 네트워크 분할 |
| CVE-2020-1472 (Zerologon) | CISA 긴급지침 20-03, MS 보안권고 | 2020.8+2021.2 패치 적용, FullSecureChannelProtection 레지스트리로 보안 RPC 강제 |

배경 노드 CVE는 CISA KEV 카탈로그(있는 경우) 또는 NIST SP 800-53 계열 일반 통제 항목과 대조.

### 7.3 프롬프트 구조

```
[입력 JSON]
{node_id, risk_score, cve, position, attack_path[], importance}

[지시]
1. 핵심 위험 1~2문장 요약
2. 즉시 조치 3가지 이내 (우선순위 포함)
3. 장기 보완조치 1~2가지
형식: JSON {summary, immediate_actions[], long_term_actions[]}
```

### 7.4 평가(검증) 방식 — 체크리스트 매칭

1. CVE별 "정답 조치 키워드" 추출 (7.2 표 기반)
2. LLM 생성 방어전략에 해당 키워드/개념 포함 여부 체크 (수동 대조 또는 키워드 매칭 스크립트)
3. **일치율(%)**로 정량화 (예: 핵심조치 3개 중 2개 포함 → 67%)

### 7.5 확장 가능한 파이프라인 설계 (노드 수 증가 대응)

- **데이터 저장**: JSONL 형식으로 노드별 결과 누적 저장 (`defense_strategies.jsonl`) — 노드 수 증가 시 파일에 줄만 추가, pandas로 즉시 분석 가능
- **평가 대상 자동 결정**: risk_score 임계값(예: 0.6) 이상인 노드를 자동으로 평가 대상에 포함 → 노드 수가 늘어나도 코드 수정 없이 평가 규모 자동 확장

```python
targets = [node for node in all_nodes if node.risk_score >= 0.6]
for node in targets:
    strategy = generate_defense(build_prompt(node))
    save_to_jsonl(strategy)
```

---

## 8. GNN 확장성 및 데이터 설계 원칙

### 8.1 GAT의 구조 유연성 (Inductive vs Transductive)

- **Transductive 방식** (일부 초기 GCN 등): 학습 시 본 그래프 전체 구조에 결과가 묶여, 구조가 바뀌면 재학습 필요
- **Inductive 방식 (GAT, 본 연구가 사용)**: 메시지 패싱 기반으로 "노드 번호"가 아닌 "feature 패턴"을 학습 → 학습 후 노드/엣지가 추가되거나 그래프 크기가 달라져도 재학습 없이 추론 가능. 4.4절에서 언급한 "동적 환경에서 GAT가 전체 재계산을 대체하는 효율성"이 바로 이 inductive 특성에 기반함

**단, 주의할 제약**: feature의 **종류/차원 자체(예: protocol 원-핫 인코딩 카테고리 수)**는 미리 고정해야 하며 사후 변경이 어려움 → "기타(Other)" 카테고리를 미리 남겨 대비 (4.2절과 연결).

**정리**: 노드/엣지 개수·그래프 구조는 유연하게 확장 가능, feature 종류/차원은 고정 필요.

### 8.2 데이터는 "양보다 위험도 분포의 균형"

배경 노드를 단순히 많이 늘리는 것은 도움이 되지 않음:
- 비슷한 저위험 노드만 반복되면 GAT가 새로운 패턴이 아니라 같은 패턴만 반복 학습 → 데이터 불균형(안전 노드 과다) 심화
- 배경 노드 라벨도 결국 우리가 정의한 risk_score 공식(4.3절)으로 계산되므로, 무분별한 양적 확장은 "공식을 자기 데이터로 재확인"하는 순환논리를 다른 형태로 재생산할 위험

**원칙**: 노드 수를 늘리기보다 **고위험/중위험/저위험이 고르게 분포하도록** 배경 노드의 CVE·속성을 설계.

### 8.3 단일 MITRE ATT&CK 체인 사용에 따른 과적합 우려 및 대응

**문제**: 현재 6단계 공격 체인(ProxyLogon→EternalBlue→Zerologon)은 하나의 스토리라인만 사용 → 모델이 이 특정 패턴에만 맞춰질 위험.

**시간 허용 시 우선 시도할 완화책**:
- 동일한 공격 스토리라인은 유지하되, **배경 네트워크 구성(노드 배치, CVE 배정)이 다른 여러 인스턴스**를 생성 (동적 그래프의 타임스텝 개념과 연계 — 같은 그래프가 시간에 따라 패치/침해되며 여러 스냅샷 생성)
- "공격 체인은 하나, 발생 맥락(배경 네트워크 상태)은 다수"로 구성해 완전한 과적합은 방지

**남는 한계 (정직하게 서술)**:
- 다른 종류의 공격 체인(랜섬웨어, 내부자 위협 등)에 대한 일반화는 본 연구 범위에서 검증하지 못함 → 결론부에 "향후 연구에서 복수의 ATT&CK 체인으로 확장 검증 필요"로 명시

> **우선순위**: 시간이 허용되는 한 8.3의 과적합 완화(다중 인스턴스 생성)를 최우선으로 시도.

---

## 9. 프로젝트 구조 (Python 기반, 웹 연동 고려)

### 9.1 디렉토리 구조

```
project/
├── core/                      # 웹과 무관한 순수 로직 (1~3단계)
│   ├── graph/
│   │   ├── network_builder.py    # 18개 노드 그래프 생성 (6장)
│   │   ├── cve_loader.py         # NVD/CVE 데이터 로딩·배정
│   │   └── scenario.py           # MITRE ATT&CK 시나리오 정의 (5장)
│   │
│   ├── detection/
│   │   ├── dijkstra.py           # 경로탐색 (Dijkstra)
│   │   ├── risk_score.py         # risk_score 라벨 생성 (4.3절 공식)
│   │   ├── gat_model.py          # GATv2 모델 정의
│   │   └── train.py              # 학습 스크립트 (오프라인 배치 실행)
│   │
│   └── defense/
│       ├── llm_client.py         # generate_defense() — 모델 교체 가능 (7.1절)
│       ├── prompt_builder.py     # 프롬프트 생성 (7.3절)
│       └── evaluator.py          # CISA 체크리스트 검증 (7.4절)
│
├── data/                      # 산출물 저장 (JSONL 등)
│   ├── graphs/                   # 생성된 그래프 스냅샷 (타임스텝별)
│   ├── models/                   # 학습된 GAT 가중치 (gat_model.pt)
│   └── defense_strategies.jsonl  # 7.5절 누적 저장 형식
│
├── api/                        # 웹과 core를 잇는 얇은 계층
│   ├── main.py                   # FastAPI 앱 진입점 — 시작 시 가중치 로드
│   └── routes.py                 # /graph, /risk, /defense 등 엔드포인트
│
├── web/                        # 프론트엔드 (시각화, 6.7절)
│   └── (React or 단순 HTML/JS)
│
└── notebooks/                  # 실험/디버깅용 주피터 노트북
```

**설계 원칙**:
1. `core/`는 웹을 전혀 몰라야 함 — 터미널에서 단독 실행/테스트 가능
2. `api/`는 `core/` 함수를 그대로 호출만 하는 얇은 다리 역할, 새 로직 추가 금지
3. `data/`가 단계 간 연결고리 — 각 단계 결과를 파일로 저장해 독립적 재실행/디버깅 가능

### 9.2 학습(Training)과 추론(Inference)의 완전한 분리

- **학습**: `core/detection/train.py`를 **오프라인 배치로 1회(또는 수 회) 실행** → 결과로 학습된 가중치 파일(`data/models/gat_model.pt`) 생성. 웹 서버와 완전히 무관하게 진행.
  - **실행 환경**: 워크스테이션 사용 가능 여부가 불확실하므로, **Google Colab(무료 GPU)을 대안 학습 환경으로 활용**. `core/`가 웹과 완전히 독립되어 있어 Colab에서도 코드 수정 없이 그대로 실행 가능
  - Colab 세션은 연결이 끊기면 파일이 소실되므로, 학습 완료 후 **가중치를 Google Drive 마운트 또는 즉시 로컬 다운로드**로 보관
  - LLM 방어전략 생성(7장)도 GPU가 필요하면 동일하게 Colab에서 배치로 미리 실행하여 `defense_strategies.jsonl`을 생성 후 다운로드 가능
- **추론(웹 서비스)**: `api/main.py` 시작 시 **이미 학습된 가중치를 로드**하고, 이후엔 새 그래프/노드가 들어올 때마다 **가벼운 계산(추론)만** 수행. 웹에서는 절대 재학습하지 않음.

```python
# api/main.py 시작 시
model = GATv2()
model.load_state_dict(torch.load("data/models/gat_model.pt"))
model.eval()  # 추론 모드 고정

# api/routes.py
@app.get("/risk/{node_id}")
def get_risk(node_id):
    return model(현재_그래프_상태)  # 가중치 변경 없이 계산만
```

이 분리는 4.4절("GAT는 학습 후 빠른 근사 추론이 목적")과 8.1절(inductive 학습 특성 — 그래프 구조가 바뀌어도 재학습 불필요)의 설계와 직접 연결됨. 학습이 끝나야 서비스가 시작될 수 있다는 순서만 지키면, 이후 두 프로세스는 서로 독립적으로 운용.

---

## 10. 작업 순서 (우선순위 기준, 날짜 대신 의존관계로 관리)

### 필수 (MVP)

| 순서 | 작업 | 의존성 |
|---|---|---|
| 1 | 네트워크 그래프 생성 코드 (18노드, NetworkX) | 없음 |
| 2 | CVE/CVSS 데이터 배정 (NVD API/수동) | 1 |
| 3 | Dijkstra 경로탐색 구현 | 1,2 |
| 4 | risk_score 라벨 생성 | 1,2 |
| 5 | GAT(GATv2) 모델 구현·학습 | 4 |
| 6 | LLM 방어전략 생성 (로컬 Qwen) | 3,5 |
| 7 | 최소 웹 시각화 (그래프+위험도 색상) | 3,5,6 |
| 8 | 보고서/발표자료 작성 | 전체 |

### 있으면 좋음 (시간 남는 순서대로)

| 우선순위 | 작업 |
|---|---|
| A | 8.3 과적합 완화 (배경 다중 인스턴스 생성) |
| B | 방어전략 CISA 체크리스트 검증 (7.4) |
| C | 시각화 고도화 (노드클릭 시 방어전략 표시 등, 계획서 6.7 전체) |
| D | 6.4 제로트러스트 2단계(별도 시나리오) |

시간이 부족해지면 A~D를 순서대로 제외, 여유 있으면 A부터 채움.

---

## 11. 남은 것 / 다음에 다룰 것

- [x] MITRE ATT&CK 공격 체인 최종 확정 및 각 단계별 CVE 배정 (5장)
- [x] 전체 네트워크 규모 및 구조 설계 방식 (6장)
- [x] risk_score의 w1/w2/w3 구체적 비중 (4.3.1, 0.3/0.3/0.4)
- [x] LLM 방어전략 프롬프트 설계 및 CISA 대조 검증 방식 (7장)
- [x] GNN 확장성, 데이터 설계 원칙, 과적합 대응 방향 (8장)
- [x] 프로젝트 구조 및 학습/추론 분리 (9장)
- [x] 작업 순서/우선순위 (10장)
- [x] 전체 파이프라인 구현 및 실행 검증 (13장 참고)
- [x] LLM을 로컬 Qwen에서 Gemini API로 전환 (13.4절)
- [ ] 보고서 작성
- [ ] (여유 시) 8.3 다중 인스턴스 과적합 완화 규모 확대, 6.4 제로트러스트 2단계

---

## 12. 참고문헌 (2026.08 기준 확인된 것)

1. Kumar, D., & Namdeo, A. K. (2025). *Adaptive Cyber Threat Prediction Using Attack Graphs and Real-Time Machine Learning Integration*. International Journal of Novel Research and Development, 10(4). — 본 연구와 가장 유사한 선행연구, 차별화 근거로 활용
2. TargetNodeRank (Springer) — 공격 그래프에 PageRank/betweenness centrality를 응용한 노드 랭킹 기법
3. VulRG (arXiv, 2025) — 직접위험(exploit likelihood) + 전파위험(propagation likelihood) 가중합 구조의 자산 위험도 산출 방식
4. Brody, S., et al. — GATv2 원논문 (표준 GAT 대비 표현력 개선)
5. Network attack knowledge inference with GCN (Nature Scientific Reports, 2025) — GATv2의 네트워크 공격 도메인 적용 사례
6. SecTracer — 실제 APT 사고 조건을 반영한 MITRE ATT&CK 매핑 시나리오 설계 사례
7. 김대형 (2025). 그래프 신경망(GNN)을 활용한 네트워크 자산 내 CVE 기반 취약점 전파 분석. 숭실대학교 대학원 석사학위논문. (기존 계획서 내 선행연구)
8. CISA AA21-062A — Mitigate Microsoft Exchange Server Vulnerabilities (ProxyLogon 공식 대응 가이드)
9. CISA — Indicators Associated With WannaCry Ransomware (EternalBlue/MS17-010 공식 대응 가이드)
10. CISA Emergency Directive 20-03 — Zerologon (CVE-2020-1472) 공식 대응 가이드

---

## 13. 구현 진행상황 및 실험 결과 (2026.08, 실제 코드 실행 기반)

이 장은 9~10장 계획을 실제로 구현하면서 나온 **결과, 발견한 문제, 수정 내역**을 기록. 방법론(설계)과 실험 로그(실제 결과)를 구분하기 위해 별도 장으로 유지.

### 13.1 최종 프로젝트 구조 (계획 대비 변경됨)

9.1절 계획에서 한 단계 더 나아가, **"누가 언제 실행하는가"** 기준으로 최상위 폴더를 재편함:

```
project/
├── app/        웹앱이 실제로 쓰는 코드 (추론 전용, 가볍고 학습·LLM 없음)
│   ├── inference.py       gat_model.pt 로드 후 위험도 추론만 수행 (학습 없음)
│   ├── defense_lookup.py  사전생성된 방어전략을 파일에서 조회 (LLM 즉석 호출 없음)
│   └── pipeline.py        Dijkstra + GAT추론 + 방어전략조회를 묶은 메인 진입점
│
├── api/        FastAPI 서버 (app/을 호출만 함)
├── web/        시각화 화면 (vis-network 기반)
│
├── core/       Colab·앱 공용 로직
│   ├── graph/network_builder.py
│   ├── detection/{dijkstra.py, gat_model.py, risk_score.py}
│   └── defense/{prompt_builder.py, evaluator.py}
│
├── train/      무거운 연산 스크립트 (GPU 필요한 것과, API 호출이라 가벼운 것이 섞여 있음 - 13.4절 참고)
│   ├── train.py       GAT 학습 — GPU 필요, Colab 전용
│   ├── evaluate.py    GAT 검증 — GPU 없어도 되지만 학습 직후 확인용이라 여기 위치
│   ├── llm_client.py  방어전략 생성 LLM 호출 — Gemini API 전환 후 로컬 실행 가능해짐
│   └── run_defense.py 방어전략 사전생성 스크립트 — 위와 동일 이유로 로컬 실행 가능
│
└── data/models/gat_model.pt, data/defense_strategies.jsonl  (생성물, git 미포함)
```

9.1절 대비 변경 이유: 프로토타입 개발 중 `core/defense/`에 있던 `run_defense.py`, `llm_client.py`가 실제로는 학습 로직이 아니라 "무거운 외부 자원을 쓰는 배치 스크립트"라는 게 명확해져, `train/`로 이동해 학습(`train.py`, `evaluate.py`)과 분리함. `core/`는 순수 그래프/알고리즘 로직만 남겨 재사용성을 높임.

### 13.2 탐지 파이프라인 구현 결과

**Dijkstra**: 6.3절에서 설계한 18노드 그래프(메인경로 5개 CVE 엣지 + 배경 13개 엣지)에 대해 실행한 결과, **메인 공격경로(attacker→web_dmz→internal_srv01→domain_controller→db_srv)를 정확히 최적 경로로 도출**함. 배경 노드들이 의도대로 "가짜경로/막다른 길" 역할을 하고 있음을 확인.

**risk_score 구현 중 발견한 버그 2건과 수정** (4.3절 공식을 실제 구현하며 드러남):

1. **접근용이성(w2) 정규화 문제**: 초기 구현(역수 후 min-max 정규화)은 attacker 자신의 비용이 0에 가까워 정규화 시 나머지 노드가 모두 0 근처로 눌리는 문제 발생. `ease = 1/(1+cost)` 형태의 bounded 변환으로 교체하여 해결.
2. **전파위험(w3) 계산 문제**: 표준 PageRank는 "얼마나 많은 링크를 받는가"(그래프 위상)만 반영해, 실제로는 안전한 배경 노드(예: file_srv)가 단순히 연결이 많다는 이유로 과대평가되는 문제 발생. **Personalized PageRank**(각 노드의 vulnerability_score를 개인화 벡터로 사용)로 교체하여, "실제로 취약한 노드로부터 전파된 위험"만 반영되도록 수정. TargetNodeRank의 "목표 기준 중요도 정교화" 취지와 일치.

수정 후 risk_score 순위는 메인경로 4개 노드(web_dmz, internal_srv01, domain_controller, db_srv)가 명확히 상위권을 형성함을 확인.

### 13.3 GAT 학습 및 검증 결과

**1차 실험 (단일 인스턴스, 18노드)**: loss는 0.073→0.016까지 감소했으나, **학습된 모델의 예측 순위에서 메인경로 노드(internal_srv01, web_dmz)가 8~10위까지 밀리고 중간 위험도 노드들과 뭉개지는 문제** 발견. 원인: 노드 수가 적어 "고위험 패턴"의 학습 예시 자체가 부족해, MSE 손실이 다수를 차지하는 중간대 노드 쪽으로 모델을 편향시킴 (regression to the mean). 8.3절에서 예견했던 과적합/데이터부족 문제가 실제로 관측된 사례.

**2차 실험 (8.3절 다중 인스턴스 완화책 적용, 30개 인스턴스·540노드)**: 동일 메인경로를 유지한 채 배경 노드 속성만 ±0.15 범위로 흔든 인스턴스 30개로 재학습. 결과:
- loss 0.0048까지 감소 (1차 대비 3배 이상 개선)
- **학습에 사용되지 않은 새 인스턴스(seed=9999)로 검증한 결과, 메인경로 4개 노드가 예측 순위 1~4위를 정확히 재현**
- **스피어만 순위상관계수 0.9835** (정답 순위와 예측 순위가 사실상 일치)

이 두 실험의 비교(아래 표)는 8.3절 과적합 완화책의 효과를 정량적으로 뒷받침하는 근거로 활용 가능:

| 실험 | 데이터 규모 | 결과 |
|---|---|---|
| 단일 인스턴스 | 18노드 | 메인경로 노드가 예측 8위까지 밀림 |
| 다중 인스턴스(30개) | 540노드 | 메인경로 정확히 1~4위, 스피어만 0.98 |

**한계**: 30개 인스턴스 모두 동일한 하나의 MITRE ATT&CK 체인(ProxyLogon→EternalBlue→Zerologon)의 배경 변형이므로, 검증된 일반화 범위는 "이 시나리오 내"로 한정됨. 다른 종류의 공격 체인에 대한 일반화는 미검증 (8.3절 한계 서술과 일치).

### 13.4 LLM 방어전략 — 로컬 Qwen에서 Gemini API로 전환

**최초 구현 (Qwen2.5-1.5B-Instruct, 로컬)**: 7.1절 계획대로 로컬 소형 모델로 구현했으나 실행 중 다음 문제 확인:
- CISA 공식 가이드 키워드 없이 프롬프트를 구성했을 때 **일치율 0%** (모델이 구체적 대응 조치를 기억하지 못하고 일반론만 생성)
- 프롬프트에 CISA 가이드를 직접 포함(RAG 방식)하도록 수정한 후 일치율이 개선되었으나 **CVE별로 0%~100% 편차가 크고, 간헐적으로 중국어가 응답에 섞이는 문제** 지속 (소형 모델의 한계로 판단)

**전환 결정**: 7.1절에 "옵션 C: 최후 수단 API"로 남겨두었던 대안을 채택. 유료 API(Claude) 대신 **Google Gemini API 무료 티어**(`gemini-flash-latest`, 신용카드 등록 불필요, 일일 요청 한도가 본 연구 규모 대비 충분)로 전환.

**전환 후 결과**: 동일 프롬프트(CISA 가이드 포함, 한국어 강제 지시 추가)로 재실행한 결과, **3개 CVE(ProxyLogon/EternalBlue/Zerologon) 모두 CISA 일치율 100%**, 언어 혼입 없음, 설명 품질도 보안 전문 용어를 정확히 사용하는 수준으로 향상.

| 비교 항목 | Qwen2.5-1.5B (로컬) | Gemini API (무료) |
|---|---|---|
| CISA 일치율 | 0~100% (편차 큼) | 100% (3개 CVE 모두 일관) |
| 언어 | 간헐적 중국어 혼입 | 한국어만 |
| 비용 | 무료(로컬 연산) | 무료(API 무료 티어) |
| 실행 위치 | GPU 권장(Colab) | 로컬에서도 실행 가능 (경량 API 호출) |

**부수 효과**: Qwen(로컬 모델 실행)은 PyTorch/transformers 최신 버전을 요구해 로컬 환경(PyTorch 2.0.0)과 반복적으로 충돌했으나, API 방식으로 전환하며 이 의존성 문제 자체가 해소됨. 결과적으로 `train/run_defense.py`는 GPU나 무거운 라이브러리 없이 **로컬에서도 실행 가능**해짐 (13.1절 구조표 참고). 진짜로 Colab(GPU)이 필요한 것은 `train/train.py`(GAT 학습) 하나로 좁혀짐.

**한계**: 무료 API 티어는 요청 한도·모델 가용성이 예고 없이 바뀔 수 있음(실제로 개발 중 `gemini-2.5-flash` 모델이 신규 사용자에게 제공 중단되는 것을 경험, `gemini-flash-latest` 별칭으로 전환해 대응). 안정적인 장기 운영에는 유료 티어 전환이 필요할 수 있음.

### 13.5 웹 시각화 구현 결과

FastAPI(`api/`) + vis-network 기반 HTML(`web/index.html`)로 6.7절 요건을 모두 구현·확인:
- 노드 위험도에 따른 색상 표시 (녹색/노랑/빨강, 6.7.3절 임계값 0.3/0.7 기준)
- 공격경로 상 노드/엣지 강조 표시 (흰 테두리, 굵은 선, 6.7.2절)
- 노드 클릭 시 방어전략(요약, 즉시조치, 장기조치, CISA 일치율) 패널 표시 (6.7.4절)

### 13.6 종합 현황

| 파이프라인 단계 | 상태 |
|---|---|
| 그래프 생성 (18노드, MITRE ATT&CK 기반) | 완료·검증 |
| Dijkstra 경로탐색 | 완료·검증 (메인경로 정확히 도출) |
| risk_score 라벨 생성 | 완료·검증 (버그 2건 수정) |
| GAT 학습·추론 | 완료·검증 (스피어만 0.98) |
| LLM 방어전략 생성 | 완료·검증 (Gemini 전환 후 CISA 일치율 100%) |
| 웹 시각화 | 완료·확인 |
| 보고서 작성 | 완료 (콜로키움용 1차본) |

---

## 14. 지도교수 피드백 반영 및 연구 재설계 (2026.08, 콜로키움 이후)

콜로키움에서 지도교수로부터 받은 피드백을 계기로, 13장까지의 구현을 "완성"이 아니라 "1차 파일럿"으로 재규정하고 연구 설계를 재점검함. 본 장은 피드백 원문의 핵심, 문헌조사 결과, 그리고 실제로 반영한/반영할 변경사항을 기록한다.

### 14.1 지도교수 피드백 요약

1. **이너 루프(inner-loop) 문제**: 네트워크 그래프도 연구자가 직접 설계하고, 학습 정답(risk_score)도 연구자가 정의한 휴리스틱 공식으로 계산한 뒤, 그 정답을 GAT가 예측하도록 학습시킴 → 데이터와 라벨이 모두 내부에서 자기참조적으로 생성되어 결과가 좋게 나오는 것이 당연하며 공정한 검증이 아님. 외부(external) 데이터로 해결해야 함.
2. **risk_score 공식(4.3절) 자체가 문제**: "고전적 방법과 최근 방법을 대충 엮어놓은 초기적 수준의 휴리스틱"이며, 실제 데이터에 적용 시 성능이 잘 나오지 않을 가능성이 높다고 지적. 권장할 만한 방법론이 아님.
3. **LLM 사용이 억지**: 실제 네트워크 상태 없이 노드 정보만 보고 그럴듯한 텍스트를 생성하는 것(할루시네이션)에 불과하며, LLM이 필요한 명확한 이유가 없음.
4. **"연구"가 되려면**: 기존 관련 연구를 먼저 찾아 각 연구가 정확히 무엇을 못했는지(한계)를 분석하고, 그 커버되지 못한 부분을 메우는 구조여야 함. "이것저것 조합하면 됩니다"는 연구가 아니라 개발/시뮬레이션.
5. **데이터**: 합성 데이터 대신 이미 존재하는 실제 데이터셋을 활용할 것.
6. (부수적 지적) Dijkstra·GAT 자체가 상대적으로 고전적인 조합이며, 제목의 "동적"을 실제로 다루는 최신 기법(시계열 그래프 모델 등)을 조사해볼 것.

### 14.2 문헌조사 결과 요약 (Advanced Research 수행)

지도교수 피드백에 대응하기 위해 심층 문헌조사를 수행함. 주요 결과:

- **선행연구 대부분이 동일한 이너루프 문제를 가짐**: 가장 가까운 선행연구 VulRG(Jiang et al., arXiv:2502.11143, 2025)조차 외부 검증 라벨 없이 CVSS·EPSS·Exploit 가중합 휴리스틱을 쓰고, 검증도 출처 논문의 정성적 코멘트와의 일치로 대체함. DSRD(Future Internet, 2026)는 아예 무학습 구조지표(Kirchhoff 유효저항)로 전환하며 스스로 "실제 침해로 검증되지 않은 확장된 개념증명"이라 명시. Lu et al.(2009)의 GNN 기반 공격그래프 순위화 연구는 본 연구와 구조적으로 거의 동일(자체 정의 랭킹 기법을 GNN이 재현)함 → **"외부에서 독립 검증된 라벨로 GNN 위험도 예측을 평가한 연구가 이 분야에 사실상 없다"는 것이 본 연구가 메울 수 있는 구체적 공백**으로 확인됨.
- **실제 사용 가능한 외부 데이터**: CISA KEV(야생 악용 확인 CVE, 이진 라벨, 무인증 공개 JSON), FIRST.org EPSS(30일 내 악용확률 0~1, 매일 갱신, 공개 API), CTID Mappings Explorer(CVE→ATT&CK→NIST 800-53 공개 매핑) 확인. DARPA TC/OpTC는 실제 레드팀 로그이나 프로세스·파일 수준 프로버넌스 그래프로 본 연구의 자산 수준 그래프와 추상화 단위가 달라 현재 범위에서는 비권장(향후 과제로 보류).
- **LLM 타당성**: 선행연구(NDSS 2026 등)는 반드시 RAG로 실제 위협 인텔/가이드라인 근거를 주입한 뒤 사용함. 근거 주입 없는 현재 방식은 학술적으로 방어 어려움. 대안은 (a) LLM 제거 후 CTID 매핑 기반 규칙기반 방어전략 생성, (b) LLM 유지 시 CISA KEV의 `requiredAction` 필드·NIST 800-53 문서를 RAG 지식베이스로 강제.
- **Dijkstra+GNN 폐루프**: 기존 연구(Kumar & Namdeo 등)는 Dijkstra와 GNN을 병렬 파이프라인으로만 쓰고, 예측 위험도가 엣지 가중치로 피드백되는 폐루프 구조는 문헌상 드묾 → 이 역시 신규성 여지.

### 14.3 즉시 반영: risk_score → 외부 라벨(EPSS/KEV) 전환

**변경 내용**: 4.3절의 risk_score 공식(w1·자기취약점 + w2·Dijkstra접근성 + w3·PageRank전파)을 학습 라벨로 사용하는 방식을 폐기하고, 대신 **FIRST.org EPSS 점수**(실제 30일 내 악용확률, 매일 갱신)와 **CISA KEV 등재 여부**(실제 야생 악용 확인)를 외부 라벨로 직접 사용하도록 전환함.

**신규 모듈**: `core/detection/external_labels.py` — EPSS API·KEV JSON을 호출해 CVE별 실측값을 가져오고 캐싱.

**정보 누설 방지 설계**: GAT 입력 feature(취약점 유형, 권한, 위치, 엣지 특성 등)에는 EPSS/KEV 값을 포함하지 않음. 정답(라벨)에만 사용해, "정답을 입력에도 넣고 맞히게 하는" 재순환을 방지.

**마스킹 기반 준지도학습**: 실제 CVE가 배정된 노드(현재 메인 경로 3개: ProxyLogon/EternalBlue/Zerologon)만 라벨을 가지며, 배경 노드는 아직 실제 CVE가 없으므로 loss 계산에서 제외(mask=False). 이는 라벨 부족을 감추지 않고 정직하게 반영한 설계다.

**1차 실행 결과** (2026.08):

| CVE | EPSS(실측) | KEV 등재 |
|---|---|---|
| CVE-2021-26855 (ProxyLogon) | 0.99999 | True |
| CVE-2017-0144 (EternalBlue) | 0.9923 | True |
| CVE-2020-1472 (Zerologon) | 0.99512 | True |

540개 노드(30개 인스턴스) 중 실제 라벨이 있는 노드 90개로 학습, loss 0.00003까지 수렴. 다만 라벨 종류가 3가지뿐이라 다양성이 부족해, **배경 노드까지 실제 CVE로 확장하는 것이 다음 우선순위 과제**로 확인됨(14.5절).

**후속 확장 (17개 노드 전체로 CVE 배정 확대) 및 발견된 사후선택 편향**: 배경 노드 14개에도 실제 CVE를 배정해 총 17개 CVE의 EPSS를 조회한 결과, 값이 0.00308(CVE-2024-0646)~0.99999 범위로 다양해졌으며 510/540 노드가 학습에 사용됨(1차 실행 결과는 loss 0.00003, 2차 확장 후에는 loss 0.01171로 다소 높아졌는데 이는 라벨 다양성이 늘어 학습이 더 어려워졌기 때문으로, 오히려 데이터 다양성이 실제로 반영되었다는 정황).

다만 이 CVE 배정 과정에서 **사후선택 편향(post-hoc selection bias)**이 발견됨: 초기에는 유명한 CVE(대부분 KEV 등재, EPSS 0.8~0.99대)만 배정한 결과 라벨이 거의 다 "매우 위험" 구간에 몰렸고, 이를 시정하기 위해 "EPSS가 낮은 CVE"를 검색 → EPSS 값을 확인 → 원하는 분포가 나오는 CVE를 선택하는 순서로 5개 노드(pc03, pc04, backup_srv, router, switch_backup)에 CVE를 배정함. 이는 **"정답 값을 먼저 보고 나서 유리한 표본을 고르는" 데이터 스누핑에 해당**하며, 애초 지도교수가 지적한 이너루프 문제와는 다르지만(라벨 자체는 여전히 외부 산출값) 검증의 공정성을 약화시키는 별도의 편향이다.

- 메인 경로 3개(ProxyLogon/EternalBlue/Zerologon)는 5장의 MITRE ATT&CK 시나리오 설계 시 EPSS를 전혀 모르는 상태로 결정되어 이 편향에서 자유로움
- 나머지 14개, 특히 마지막 5개는 역할-CVE 정성적 적합성 기준으로 골랐다고 하나 완전히 결백하지 않음
- **향후 개선 방향**: 노드 역할에 맞는 CVE 후보군을 EPSS 확인 없이 먼저 확정(예: NVD API로 카테고리별 후보 10~20개 추출) → 그중 무작위 배정 → 이후에만 EPSS 조회하는 순서로 재실험 필요. 시간 제약상 본 단계에서는 미반영, 한계로 기록만 함.

### 14.4 GAT(정적 모델) 유지 여부에 대한 결정

지도교수의 "Dijkstra·GAT가 고전적"이라는 부수적 지적에 대해 시계열 그래프 모델(TGN, EvolveGCN 등)로의 전환을 검토했으나, 다음 이유로 **현 단계에서는 GATv2를 유지**하기로 결정함:

1. 라벨을 EPSS/KEV로 바꾼 지 얼마 안 되어 아직 라벨 자체가 불안정(3종류뿐) → 이 상태에서 모델까지 바꾸면 결과 변화의 원인(라벨 문제 vs 모델 문제)을 구분할 수 없음
2. 지도교수 피드백의 핵심(이너루프, LLM 근거 부족)이 모델 구조보다 데이터/라벨/검증 방식에 있었음
3. 시계열 GNN 전환은 데이터 파이프라인(타임스텝별 그래프 스냅샷 생성)부터 다시 설계해야 하는 큰 작업

**결정**: 1단계(현재)는 GATv2 + 외부 라벨로 검증 가능한 결과를 확보하고, 2단계(향후 확장)로 시계열 GNN 전환을 명시적으로 남겨둠. 이는 "제목의 동적성을 아직 모델 수준에서 완전히 구현하지 못했다"는 한계를 정직하게 인정하는 것이기도 함.

### 14.6 최종 전환: vuln-net-ag(검증된 생성기) 기반 실제 데이터셋으로 이동

14.3~14.5절의 과정(EPSS/KEV 외부라벨, CVE 확장, 사후선택 편향 발견)을 거치며, "네트워크 토폴로지+CVE 배정까지 연구자가 직접 설계하는 한" 편향 문제가 완전히는 해소되지 않는다는 한계가 드러남. 이에 **Palma & Bonomi(Computers & Security, 2025)의 vuln-net-ag**(실제 NVD 데이터 기반 취약 네트워크 생성기, 실제 3개 조직 네트워크와의 유사성을 저자가 검증)로 그래프 생성 자체를 전환함.

**적용 과정**:
1. `vuln-net-ag` 저장소 clone, `main_vulnet.py`로 생성된 `networks/*.json`(호스트·서비스·CVE 인벤토리) 및 `main_ag.py`로 생성된 NETSPA/TVA 공격그래프(graphml) 확보
2. NETSPA/TVA graphml은 노드가 "권한상태/선행조건/취약점"으로 세분화된 착취 의존성 그래프 구조라 우리 파이프라인(호스트=노드)과 맞지 않아 **참고자료로만 보류**, 대신 `networks/*.json`(호스트 단위 devices/edges)을 채택
3. `core/graph/vulnet_loader.py` 신규 작성: devices의 CVE 중 최고 CVSS를 노드 대표값으로 선정(실제 NVD 응답에서 직접 추출, 임의 배정 없음), 엣지의 attack_cost/success_prob는 목적지 노드의 실제 CVSS·EPSS로 산출
4. `train/train.py`를 vuln-net-ag의 실제 네트워크 파일(5호스트 규모 18개)로 재학습

**결과**: 18개 네트워크, 총 90개 노드 **전원**이 실제 EPSS 라벨을 가짐(라벨 커버리지 100%, 이전 단계의 3/540→510/540 대비 큰 개선이자 마스킹 자체가 불필요해짐). loss는 0.215→0.051로 수렴.

**이 전환이 지도교수 피드백을 해소하는 방식**:
- 이너루프(1번): 그래프 구조·CVE 배정을 연구자가 전혀 만들지 않음(생성기가 실제 NVD 분포 기반 자동 배치) → 라벨(EPSS)도 외부 → 이중으로 외부화됨
- 사후선택 편향(14.3절에서 자체 발견한 한계): 연구자가 어떤 CVE를 볼지 고르는 단계 자체가 없어짐 → 구조적으로 재발 불가능
- 합성 데이터 지적(5번): 생성기 저자가 실제 3개 조직 네트워크와의 유사성을 별도 검증한 도구를 사용 → "완전한 합성"에서 "검증된 현실적 생성기 기반"으로 격상

**한계**: (a) 지금은 5호스트 규모 18개 인스턴스만 사용, 저장소에는 10/50호스트 규모까지 총 106개(NETSPA/TVA 포함 시 그 이상) 존재하므로 규모 확장 여지가 큼. (b) NETSPA/TVA의 정교한 착취 의존성 그래프(권한 단계별 세분화)는 아직 미활용 — 향후 이 구조를 직접 다루는 방향으로 재작업하면 더 정교한 경로 분석이 가능하나 대대적인 재설계가 필요. (c) MITRE ATT&CK 기반 서사(ProxyLogon→EternalBlue→Zerologon)는 이 전환으로 사라짐 — "특정 실제 사고 재현"에서 "일반화된 실제 데이터 기반 검증"으로 연구의 중심이 이동함을 서론/결론에 명시할 필요.

### 14.8 규모 확장 실험: 학습 데이터 크기와 일반화 성능의 관계

14.6절 직후 5호스트 네트워크(18개, 90노드)만으로 held-out 검증(10호스트, 학습에 미사용)을 수행한 결과 스피어만 상관계수 0.17로 사실상 무작위 수준이었고, GAT 예측값이 0.24~0.25 구간에 몰려 나오는 회귀-평균 현상이 재현됨. 이는 90개 노드로는 일반화 가능한 패턴 학습에 데이터가 부족함을 시사.

학습 데이터를 5호스트+10호스트(36개 네트워크, 270노드)로 확장하고 held-out을 50호스트(18개 네트워크, 900노드, 학습에 전혀 사용되지 않은 규모)로 재검증한 결과, 스피어만 상관계수가 **0.8547**로 크게 개선됨.

| 실험 | 학습 규모(노드) | Held-out 검증 대상 | 스피어만 |
|---|---|---|---|
| 1차 | 90 (5호스트만) | 10호스트 (미사용 규모) | 0.1714 |
| 2차 | 270 (5+10호스트) | 50호스트 (미사용 규모) | 0.8547 |

이는 8.3절/14.6절에서 예상한 "데이터 규모가 GAT 일반화의 핵심 병목"이라는 가설을 vuln-net-ag 실제 데이터로 재현·검증한 결과다. 다만 예측값의 절대 스케일이 실제값보다 압축되어 나오는 경향(예: 실제 EPSS 1.0인 노드에 예측 0.59)이 남아 있어, 순위 판별력은 확보되었으나 절대 위험도 수치의 보정(calibration)은 향후 개선 과제로 남음.

**다음 개선 방향(우선순위순)**: (1) 50호스트 네트워크 일부(topology 기준 분할)까지 학습에 포함하고 검증셋을 재구성해 규모를 한 번 더 확장, (2) 학습 epoch·patience 확대 및 손실함수 재검토(sigmoid 포화 구간에서의 기울기 소실 대응), (3) CWE(vulnerabilities 필드에 이미 포함된 실제 NVD weakness 분류)를 노드 입력 특성에 원-핫으로 추가해 입력 신호 보강, (4) 저장소 전체 106개 네트워크로 확장.

### 14.10 모델·손실함수 개선: GATv2→Graph Transformer, MSE→BCE

14.8절 결과(스피어만 0.85→0.79, 검증 난이도를 높였을 때)에서 예측값이 실제값보다 압축되어 나오는 문제(예: 실제 EPSS 1.0에 예측 0.30)가 관찰됨. 원인 분석 결과 두 가지 병목 확인:

1. **sigmoid 포화 문제**: 출력층에 sigmoid를 적용한 뒤 MSE로 학습했는데, EPSS 값 중 0.99999 같은 극단값이 많아 sigmoid의 극단 구간(기울기가 0에 가까움)에서 학습이 정체됨
2. **GAT의 제한적 어텐션 범위**: GATv2는 1-hop 이웃에 대해서만 어텐션을 계산하는 구조로, 지도교수가 지적한 "더 최신 GNN 구조 검토" 요청과도 관련됨

**개선 조치**:
- 모델: `RiskGAT`(GATv2Conv 기반) → **`RiskGraphTransformer`**(TransformerConv 기반, Query-Key-Value 어텐션으로 GAT보다 넓은 표현력) 신규 구현·전환
- 손실함수: `MSELoss`+출력층 sigmoid → **`BCEWithLogitsLoss`**(raw logit에 직접 적용, 극단값 근처에서도 기울기 유지). 추론 시에만 `torch.sigmoid()`로 0~1 확률로 변환

**결과**: 동일한 held-out 조건(50호스트 star 토폴로지, 학습 시 미사용)에서 스피어만 상관계수 0.7950 → **0.9404**로 개선, 예측값 압축 문제도 시각적으로 해소됨(실제-예측 쌍이 대각선에 가깝게 정렬).

| 실험 | 모델 | 손실함수 | Held-out 스피어만 |
|---|---|---|---|
| 3차 | GATv2 | MSE + sigmoid | 0.7950 |
| 4차 | Graph Transformer | BCE (raw logit) | **0.9404** |

**한계**: 모델 교체와 손실함수 교체를 동시에 적용해, 두 변경 중 어느 쪽이 개선에 더 크게 기여했는지는 분리 검증하지 못함(→ 14.10.1절에서 해소).

### 14.10.1 Ablation 실험: 모델과 손실함수의 개별 기여도 분리

모델(GATv2/Graph Transformer) × 손실함수(MSE+sigmoid/BCE) 4개 조합을 모두 학습·동일 held-out(50호스트 star 토폴로지)으로 검증함.

| 조합 | Held-out 스피어만 |
|---|---|
| Graph Transformer + BCE | **0.9403** |
| GATv2 + BCE | 0.9112 |
| GATv2 + MSE | 0.7860 |
| Graph Transformer + MSE | **0.3651** (최저) |

**핵심 발견**: 손실함수(BCE)가 모델 구조 교체보다 성능에 훨씬 결정적인 요인임. BCE를 쓴 두 조합(0.91, 0.94)은 모두 강한 성능을 보인 반면, MSE를 쓴 두 조합(0.79, 0.37)은 모두 약함. 특히 **표현력이 더 큰 Graph Transformer가 잘못된 손실함수(MSE+sigmoid)와 결합하면 오히려 더 단순한 GATv2보다도 성능이 크게 저하됨**(0.37 vs 0.79) — 모델의 표현력이 클수록 손실함수의 기울기 소실 문제에 더 민감하게 반응하는 것으로 해석됨. 즉 "최신 아키텍처 도입" 자체보다 "출력-손실함수 정합성"이 선행되어야 아키텍처 개선의 이득을 볼 수 있음을 실증. 이 결과로 지도교수의 "더 최신 GNN 구조 검토" 요청에 대해, **단순 아키텍처 교체보다 손실함수 설계가 우선한다는 정량적 근거를 확보**함.

### 14.10.3 참고: 공격그래프+GNN 분야에서 GAT의 위치, Dijkstra의 최신 대안

**GAT가 "낡은 선택"인가에 대한 재확인**: GNN-IDS(Sun et al., ARES 2024)는 MulVAL로 생성한 공격그래프에 GCN, GCN-EW, GAT를 직접 비교 대상으로 사용했으며, UAV 보안 연구(Majumder et al.)에서도 귀납적(inductive) 모델인 GAT와 GraphSAGE가 LSTM 및 전이적(transductive) 모델보다 우수한 성능을 보인 것으로 보고됨. 반면 이 좁은 분야(공격그래프 위험도 예측)의 상당수 선행연구(VulRG, DSRD, TargetNodeRank)는 GNN 자체를 사용하지 않고 PageRank·전기회로이론 등 무학습 방식으로 우회함. 종합하면 **GATv2는 이 분야에서 여전히 표준적으로 비교되는 선택지**였으며, 14.10절의 Graph Transformer 전환은 표준을 넘어선 개선 시도로 위치지을 수 있음.

**Dijkstra의 최신 대안**: 강화학습(RL) 기반 공격경로 탐색이 활발히 연구되는 대안으로 확인됨. Terranova et al.(RAID 2024)은 "현재의 공격경로 예측 방법은 대부분 기존 전문가 지식이나 정해진 휴리스틱에 의존하며, 이런 전통적 방법은 대규모의 매우 취약한 네트워크를 다룰 때 특히 어려워진다"고 지적하며 DRL(심층강화학습) 기반 대안을 제시함. ShotFlex(Yu et al., 2025) 등도 RL 기반 공격경로 생성을 다룸. RL 방식은 Dijkstra처럼 고정 가중치의 단일 최적경로를 계산하는 대신, 에이전트가 시뮬레이션 환경에서 탐지회피·시간·성공률 등 복수 목표를 동시에 고려하며 학습한다는 점에서 차별화됨. 다만 RL 전환은 별도의 시뮬레이션 환경·보상함수 설계가 필요해 상당한 재작업을 요하므로, 본 연구에서는 **14.12절의 장기 확장 과제**로만 남겨둠.

### 14.11 LLM 재검토 — 결론: 기존 설계가 이미 "근거 주입" 요건을 충족함

지도교수 피드백(3번, LLM 근거 부재)에 대응하기 위해 두 대안을 검토함: (A) LLM을 제거하고 CTID Mappings Explorer(CVE→ATT&CK→NIST 800-53 공개 매핑) 기반 규칙기반 매핑으로 완전 대체, (B) LLM을 유지하되 RAG로 실제 가이드라인 근거 주입을 강제.

CTID의 `mappings-explorer` 저장소(Apache-2.0, MITRE 공개)에 원본 매핑 데이터가 실제로 존재함을 확인했으나, CVE→CWE→CAPEC→ATT&CK→NIST 800-53 전 구간을 자동 연결하는 파이프라인 구축은 스키마 조사부터 필요해 상당한 시간이 소요될 것으로 판단됨.

**재검토 결과, 기존에 구현된 설계(7.2~7.4절, 13.4절)가 이미 대안 (B)의 요건을 충족하고 있음을 확인**: 프롬프트에 CISA 공식 대응 가이드(`requiredAction`에 해당하는 내용)를 직접 삽입해 생성을 강제하고, 생성 결과를 동일한 CISA 가이드와 재대조해 일치율을 정량 검증하는 구조는 NDSS 2026 선행연구가 요구하는 "실제 위협 인텔리전스/가이드라인 근거 주입(RAG) 후 생성" 패턴과 부합함. 13.4절에서 이미 CISA 일치율 100%(Gemini 전환 후)를 실증한 바 있어, "근거 없는 할루시네이션"이라는 지적에 대한 실증적 반박 근거로 사용 가능.

**결론**: 현재 설계를 유지하고, CTID 데이터 기반 완전 자동화(대안 A, CVE 배정마다 자동으로 정답 체크리스트 생성)는 향후 확장 과제로 남김. 다만 지금은 CVE 3개(메인경로)에 대해서만 수동으로 정답표(7.2절)를 작성했다는 한계는 남아 있으며, vuln-net-ag 기반 대규모 네트워크(14.6절 이후)로 전환한 상황에서는 CVE 종류가 훨씬 많아지므로 자동화의 필요성이 커짐 — 규모가 커질수록 대안 A의 우선순위가 높아짐을 명시.

### 14.13 Dijkstra-GAT 폐루프 구현: GAT 위험도의 엣지 가중치 피드백

1.1절에서 Kumar & Namdeo와의 차별점으로 제시했던 "GNN 위험도가 엣지 가중치에 반영되어 Dijkstra가 재실행되는 실질적 결합"을 실제로 구현함.

**구현**: `app/pipeline.py`에 `apply_gat_feedback()` 함수 추가. GAT가 예측한 각 노드의 위험도(0~1)를 이용해, 해당 노드로 들어오는 엣지의 attack_cost를 `기존비용 × (1 - 0.5×위험도)`로 할인(최대 50%)한 새 그래프를 생성하고, 이 그래프로 Dijkstra를 재실행해 "원래 경로"와 "GAT 반영 경로"를 비교.

**실험 1 (기존 18노드 그래프)**: 원래 경로와 GAT 반영 경로가 동일하게 유지됨(`attacker→web_dmz→internal_srv01→domain_controller`). GAT 예측 위험도 순위는 대체로 실제 설계 의도와 일치했으며(web_dmz 1위, domain_controller 상위권), 특히 배경 노드였던 `vpn_dmz`가 예측 위험도 0.52로 메인경로 노드 `internal_srv01`(0.51)보다 높게 나와 GAT가 "숨은 위험"을 포착하는 사례를 확인함.

**실험 2 (우회경로 추가 검증)**: vpn_dmz→domain_controller 우회 엣지를 추가해 폐루프가 실제로 경로를 뒤집을 수 있는지 검증. 계산 결과 원래 경로(누적비용 약 0.08)와 우회경로(약 0.49)의 격차가 6배 이상으로 커서, GAT 피드백의 최대 할인(50%)을 적용해도 순위 역전에는 이르지 못함(할인 후에도 약 4배 차이 잔존).

**해석**: 폐루프 메커니즘 자체는 정상 작동함(엣지 비용이 실제로 변화)이 확인되었으나, 원래 그래프의 비용 설계가 견고할 경우 GAT 피드백만으로 경로 순위를 뒤집기에는 부족할 수 있음을 실증. 이는 오히려 **"GAT 피드백이 근거 없이 경로를 흔들지 않고, 원래 설계가 충분히 견고하면 유지된다"는 안정성 측면의 정직한 결과**로 해석 가능. 할인 폭을 더 키우거나(예: 최대 100%), 애초에 비용 격차가 더 작은 시나리오에서 재검증하는 것을 향후 과제로 남김.



### 14.14 CWE(취약점 유형) 원-핫 특성 추가

지금까지 GAT 입력 노드 특성은 5개 숫자(취약점점수·권한·중요도·침해여부·패치상태)뿐이었음. vuln-net-ag의 `vulnerabilities` 필드에 이미 포함된 NVD 실제 `weaknesses`(CWE 분류)를 API 호출 없이 추출해 14종 CWE + OTHER의 원-핫 벡터로 입력 특성에 추가함(`core/graph/vulnet_loader.py`, `core/detection/gat_model.py` 확장).

동일 조건(48개 네트워크 학습, 50호스트 star held-out)으로 4개 조합 재검증:

| 조합 | CWE 추가 전 | CWE 추가 후 |
|---|---|---|
| Graph Transformer + MSE | 0.3651 (최저) | **0.9457 (최고, 대폭 개선)** |
| Graph Transformer + BCE | 0.9403 | 0.9400 (유지) |
| GATv2 + BCE | 0.9112 | 0.8949 (소폭 하락) |
| GATv2 + MSE | 0.7860 | 0.8640 (개선) |

**핵심 발견**: 14.10.1절에서 "손실함수(BCE)가 모델 구조보다 결정적"이라고 결론 내렸던 것에 중요한 조건이 추가됨 — **입력 특성이 빈약할 때는 손실함수 선택이 성능을 좌우하지만, CWE라는 풍부한 도메인 신호가 추가되면 손실함수 간 격차가 사실상 사라짐**(Transformer 기준 MSE 0.95 vs BCE 0.94, 거의 동률). 특히 이전 실험 최악이었던 Transformer+MSE 조합이 이번엔 전체 1위로 역전된 것은, sigmoid 포화라는 손실함수 자체의 결함을 "더 나은 입력 특성 제공"으로 우회할 수 있음을 보여줌. 즉 아키텍처·손실함수 개선보다 **도메인 지식을 반영한 입력 특성 설계가 더 근본적인 성능 개선 레버**일 수 있음을 실증.

**최종 모델 선정**: Transformer+MSE(0.9457)와 Transformer+BCE(0.9400)가 근소한 차이로 동급 최상위. 다만 BCE는 sigmoid 포화 문제에 대한 이론적 강건성이 더 높으므로, 향후 안정성이 중요한 배포 시나리오에서는 Transformer+BCE를 기본값으로 유지하는 것을 권장.


### 14.15 다음 우선순위 (갱신)

1. ~~배경 노드도 실제 CVE로 확장~~ — **완료**(14.3절), 이후 vuln-net-ag 전환으로 대체
2. ~~vuln-net-ag 통합~~ — **완료**(14.6절)
3. ~~held-out 네트워크로 GAT 일반화 검증~~ — **완료**(14.8절), 스피어만 0.17→0.85
4. ~~규모 추가 확장 + 모델/손실함수 개선~~ — **완료**(14.10절), 스피어만 0.79→0.94
5. ~~Ablation(모델·손실함수 개별 기여도 분리)~~ — **완료**(14.10.1절), 손실함수(BCE)가 모델 교체보다 결정적임을 확인
6. ~~LLM 재검토~~ — **완료**(14.11절), 기존 설계가 RAG 근거주입 요건 충족함을 확인, CTID 완전자동화는 향후과제로 보류
7. ~~선행연구 한계 분석을 방법론에 명시적으로 반영~~ — **완료**(1.1.1절), 7개 선행연구 비교표로 3.1절 격상 편입
8. ~~CWE 원-핫 특성 추가~~ — **완료**(14.14절), Transformer+MSE 0.37→0.95로 대반전, "손실함수보다 입력특성 설계가 우선"이라는 통찰 확보
9. ~~Dijkstra-GAT 폐루프 구현~~ — **완료**(14.13절), 메커니즘 정상 작동 확인하되 경로 역전에는 미치지 못함(비용격차 문제)을 정직하게 기록
10. **(장기 확장) NETSPA/TVA 착취 의존성 그래프 구조로 재작업, 시계열 GNN 전환, 강화학습(RL) 기반 공격경로 탐색으로 Dijkstra 대체 검토** (14.10.3절)
11. **(규모 커지면 우선순위 상승) CTID 기반 방어전략 검증 완전 자동화**

---